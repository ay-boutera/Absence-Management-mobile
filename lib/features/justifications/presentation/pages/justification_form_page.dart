import 'package:flutter/material.dart';

class JustificationFormPage extends StatelessWidget {
  const JustificationFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Justification Form'),
      ),
    );
  }
}
